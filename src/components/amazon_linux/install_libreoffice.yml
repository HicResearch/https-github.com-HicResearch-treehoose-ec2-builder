

name: InstallLibreoffice
description: this document installs libreoffice on Amazon Linux 2
schemaVersion: 1.0

phases:
    - name: build
      steps:
        - name: InstallLibreoffice
          action: ExecuteBash
          inputs:
            commands:
                - sudo amazon-linux-extras install -y libreoffice
